import { AppHeader, Check, Screen } from "../ui";

const groups=[{title:"꼭 챙겨요",items:[["모바일 티켓",true,""],["신분증",true,"본인 확인용"],["응원봉",true,""]]},{title:"AI가 추천했어요",items:[["보조배터리",false,"공연 중 배터리 부족 대비"],["접이식 우산",false,"오후 9시 비 예보"],["생수",false,"500ml 이하"]]}];
export default function ChecklistPage(){return <Screen><AppHeader title="준비물 체크" back="/" action={<button>편집</button>}/><main className="screen-body no-nav checklist-body"><section className="check-progress"><div><span>준비 완료까지</span><strong>2개 남았어요</strong></div><b>60%</b><i><em/></i></section>{groups.map((g,gi)=><section className="check-group" key={g.title}><div className="check-group-head"><h2>{g.title}</h2>{gi===1&&<span>✦ 날씨·행사 맞춤</span>}</div>{g.items.map(([name,done,meta])=><label className={done?"done":""} key={String(name)}><input type="checkbox" defaultChecked={Boolean(done)}/><Check done={Boolean(done)}/><div><strong>{name}</strong>{meta&&<p>{meta}</p>}</div>{name==="접이식 우산"&&<span className="weather-tag">날씨 추천</span>}</label>)}</section>)}<button className="add-item">＋ 준비물 직접 추가</button><aside className="offline-note"><span>↓</span><p><strong>오프라인에서도 확인 가능해요</strong><br/>체크리스트는 기기에 안전하게 저장돼요.</p></aside></main></Screen>}
